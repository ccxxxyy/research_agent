"""MCP (Model Context Protocol) tool servers.

Each server exposes tools via the MCP protocol, enabling protocol-enforced
tool calling instead of prompt-driven tool calling. Agents discover and
invoke tools through langchain-mcp-adapters.
"""
